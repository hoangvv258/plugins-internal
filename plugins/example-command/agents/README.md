# Agents Directory

This directory contains definitions for specialized Copilot/Claude agents.
Each agent should be defined with its own configuration (e.g., Markdown or JSON schema) specifying its bounded context, custom instructions, and available toolsets.

**Example Agent Scenario:**
An `architect-agent` that restricts itself strictly to high-level system design while possessing exclusive access to architecture documentation retrieval skills.
